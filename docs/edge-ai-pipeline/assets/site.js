const menu=document.querySelector('.menu'),side=document.querySelector('aside');menu.addEventListener('click',()=>{const open=side.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));});document.querySelector('#print').addEventListener('click',()=>window.print());document.addEventListener('keydown',e=>{if(e.key==='Escape'){side.classList.remove('open');menu.setAttribute('aria-expanded','false');}});
const search=document.querySelector('#catalog-search');if(search){search.addEventListener('input',()=>{const query=search.value.trim().toLowerCase();let visible=0;document.querySelectorAll('.catalog-section').forEach(section=>{const title=section.querySelector('h2').textContent.toLowerCase();let any=false;section.querySelectorAll('li').forEach(item=>{const show=title.includes(query)||item.textContent.toLowerCase().includes(query);item.hidden=!show;if(show)any=true;});section.hidden=!any;if(any)visible++;});document.querySelector('#empty').hidden=visible>0;});}